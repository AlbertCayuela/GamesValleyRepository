import 'package:flutter/material.dart';

class LanguageDeleteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Edit you languages'),
        centerTitle: true,
      ),
    );
  }
}
